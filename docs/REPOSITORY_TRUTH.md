# Repository Truth — Historical Development Record

> **Historical document.** This file records an earlier V2 development checkpoint and is retained for auditability. It does not describe the final release state. For the final reviewer path and validation results, use `README.md` and `RUN.md`.

The original submitted V1 commit is:

`02868bc10e735bda361d4f48d1300a569f85647b`

V2 was developed from that baseline through a sequence of semantic-memory, retrieval, lifecycle and grounded-answer hardening checkpoints.

Historical branch names, commit identifiers and test counts elsewhere in this repository intentionally remain preserved inside checkpoint reports. They should not be interpreted as the final release HEAD.

The final release adds model-backed longitudinal memory with structured lifecycle state, provenance, semantic embeddings, entity-aware retrieval, temporal constraints, scoped preferences, forgetting boundaries and grounded Hey Kivi realization.

Final reviewer-facing validation is summarized in:

- `README.md`
- `RUN.md`
- `eval/final-hardening-clean-500/`
- `eval/post500-semantic-challenge-post-hardening/`

Historical development reports remain under `docs/` and `eval/` so failures and intermediate results are not erased.

The original V1 commit remains part of repository history. Historical user-authored/product documents and earlier evaluation artifacts are retained where useful for auditability.
